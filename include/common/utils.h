#ifndef UTILS_H
#define UTILS_H

/**
 * @brief Duplicate a string
 *
 * @param str The string to duplicate
 * @return char* A new string that is a duplicate of the input string, or NULL if memory allocation failed
 */
char *my_strdup(const char *str);

#endif /* UTILS_H */